extends layouts/main
include mixins/videoBlock

block content
    .home-videos
        each item in videos
            +videoBlock({
                id:item.id,
                views: item.views,
                title: item.title,
                videoFile: item.fileUrl
            })